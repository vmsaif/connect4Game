to make this work on a terminal on a linux based machine, 

To Compile (in terminal):

javac *.java


To Run(in terminal):
java Main

Note: I have modified the consol view of TextUI a slightly so that 
it is large, easier to see, and added column numbers so that 
the human player doesn't have to count the column every time 
before making a play.  